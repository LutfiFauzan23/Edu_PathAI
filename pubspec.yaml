name: edu_path
description: "EduPath AI - Platform Perencanaan Pendidikan & Karier Siswa"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fl_chart: ^1.1.0
  http: ^1.2.0
  firebase_core: ^2.14.0
  firebase_auth: ^4.6.2
  google_sign_in: ^6.1.4 
  flutter_facebook_auth: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
